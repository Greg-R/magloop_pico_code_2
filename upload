#  Script to upload magloop code to Pi Pico.
#!/bin/bash

# Command when using Raspberry Pi GPIOs:
#openocd -f interface/raspberrypi-swd.cfg -f target/rp2040.cfg -c "program build/magloop_pico.elf verify reset exit"


# Using debug probe:
sudo openocd -f interface/cmsis-dap.cfg -f target/rp2040.cfg -c "adapter speed 5000" -c "program build/magloop_pico.elf verify reset exit"
